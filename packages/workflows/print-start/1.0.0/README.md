# print-start

Typed workflow placeholder package for the registry fixture.

This package is intentionally minimal and demonstrates the workflow payload
shape consumed by `package-model` validation in this repository's early
workflow plumbing.
