package:
  namespace: devices
  name: legacy-probe
  version: 1.0.0
  kind: device

device:
  class: probe
  model: legacy

requires:
  connector: analog_input
  voltage_domains:
    - logic_5v

dependencies:
  chips/generic-mcu:
    version: "<1.1"
